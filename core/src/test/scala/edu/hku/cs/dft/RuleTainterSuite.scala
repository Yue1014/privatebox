package edu.hku.cs.dft

//import breeze.linalg.DenseVector
import edu.hku.cs.dft.TrackingMode.TrackingMode
import edu.hku.cs.dft.tracker.{RuleTainter, TrackingPolicy, TrackingType}

/**
  * Created by jianyu on 4/14/17.
  */
object RuleTainterSuite {

  def main(args: Array[String]): Unit = {
//    val ruleTainter = new RuleTainter(new TrackingPolicy(TrackingType.KeyValues, TrackingMode.FullTracking, true), null)
////    val denseVector = DenseVector[Int](1, 2, 3, 4, 5, 6)
//    val l = List(1, 2, 3, 4)
////    val taintedList = ruleTainter.setTaint(denseVector)
////    val plusTaint = taintedList.map(t => t + 1)
//
////    val taintedList = denseVector.map(ruleTainter.setTaint)
//    val k = 1
  }

}
